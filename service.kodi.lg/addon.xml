<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<addon id="service.kodi.lg" version="1.0.9" name="Kodi LG" provider-name="Unrefundable">
    <requires>
        <import addon="xbmc.python" version="3.0.0"/>
    </requires>
    <!-- Background service: installs keymap on startup and watches for events -->
    <extension point="xbmc.service" library="service.py" start="login"/>
    <!-- Script entry point: called by keymap for seek actions -->
    <extension point="xbmc.python.script" library="default.py"/>
    <extension point="xbmc.addon.metadata">
        <summary lang="en_GB">Kodi LG – LG remote enhancements for Kodi</summary>
        <description lang="en_GB">
            Enhances Kodi for LG TV users connected via HDMI-CEC.

            Feature 1 – OSD on Up/Down:
            Remaps the Up and Down navigation buttons during video playback to
            show the OSD progress bar and pause controls instead of the default
            10-minute skip forward/back behaviour.

            Feature 2 – Seek on button release:
            Fullscreen fast-forward and rewind now accumulate while the button
            is held, then perform a single seek when the button is released.
            This avoids repeated stream reloads during long seeks.
        </description>
        <platform>all</platform>
        <assets>
            <icon>resources/icon.png</icon>
            <fanart/>
        </assets>
    </extension>
</addon>
